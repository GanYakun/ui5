apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gbmshelm.fullname" . }}-test-connection"
  labels:
    {{- include "gbmshelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gbmshelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
