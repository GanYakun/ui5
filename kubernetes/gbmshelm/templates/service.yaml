apiVersion: v1
kind: Service
metadata:
  name: {{ include "gbmshelm.fullname" . }}
  labels:
    {{- include "gbmshelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gbmshelm.selectorLabels" . | nindent 4 }}
