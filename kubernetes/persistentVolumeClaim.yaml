apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gbms-pv-claim
  namespace: gbms
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi